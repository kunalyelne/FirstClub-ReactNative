/**
 * Metric Type Enum
 * Domain Layer - Domain Enumeration
 */

/**
 * Health metric types available in the app
 */
export enum MetricType {
  STEPS = 'Steps',
  CALORIES = 'Calories',
  WATER = 'Water',
  SLEEP = 'Sleep',
  HEART_RATE = 'Heart Rate',
}
